import type { ReactNode } from 'react'

export type BlogSearchResultsListSearchProps = {
  children: ReactNode
}

export const BlogSearchResultsListSearch = ({ children }: BlogSearchResultsListSearchProps) => {
  return <>{children}</>
}
