import type { ReactNode } from 'react'

export type MiniSectionCtaSubtitleProps = {
  children: ReactNode
}

export const MiniSectionCtaSubtitle = ({ children }: MiniSectionCtaSubtitleProps) => {
  return <>{children}</>
}
