import type { ReactNode } from 'react'

export type GlobalSearchResultsListSearchProps = {
  children: ReactNode
}

export const GlobalSearchResultsListSearch = ({ children }: GlobalSearchResultsListSearchProps) => {
  return <>{children}</>
}
